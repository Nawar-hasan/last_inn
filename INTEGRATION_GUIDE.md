# LearnWorlds Integration Guide - Innovologia Platform

## ✅ تم الربط بنجاح مع LearnWorlds API

تم ربط المنصة بالكامل مع LearnWorlds API وإعداد جميع الوظائف الأساسية.

## 🔑 البيانات المستخدمة

### معلومات المدرسة (School Information)
- **School ID**: `68ed729611c5dc597abde4c9`
- **School Domain**: `innovologia.learnworlds.com`
- **Admin API URL**: `https://innovologia.learnworlds.com/admin/api/v2`

### مفاتيح API (API Keys)
- **Admin Token**: `I9KZFaZKmtnMf3rYf4VsaaS0a29VOsySY3NtFQyI`
- **Client ID**: `69463d4a0f54e8bf3e0747a5`
- **Client Secret**: `Wy1rhLAAjfodbbEzPcLuKHhHb3GZX8BS44TYRH0Kh6WfWdn7sS`

### Webhook
- **Webhook Signature**: `2047aa629a9e605817f9f9a7b491c286bf6ce885f30bad`

## 📋 الوظائف المُفعّلة

### 1. نظام المصادقة (Authentication)
✅ **تسجيل الدخول (Login)**
- دعم تسجيل الدخول عبر البريد الإلكتروني وكلمة المرور
- تكامل مع LearnWorlds SSO
- إدارة الجلسات (Sessions) مع Cookies آمنة
- تسجيل الدخول عبر Google, Facebook, LinkedIn, Apple

✅ **التسجيل (Registration)**
- إنشاء حسابات جديدة عبر LearnWorlds API
- التحقق من المستخدمين الموجودين
- دعم OAuth2 مع مزودي الخدمة الاجتماعية

✅ **إدارة الجلسات (Session Management)**
- حفظ بيانات المستخدم في Cookies آمنة
- صلاحية الجلسة 24 ساعة
- تحديث تلقائي للبيانات

### 2. الدورات التدريبية (Courses)
✅ **عرض الدورات**
- جلب جميع الدورات من LearnWorlds
- عرض تفاصيل الدورة الكاملة
- الصور والأسعار والمدة

✅ **التسجيل في الدورات**
- التحقق من التسجيل الحالي
- إضافة المستخدم للدورة
- متابعة التقدم

### 3. نظام الدفع (Payments)
✅ **معالجة الدفع**
- إنشاء جلسات دفع عبر LearnWorlds
- إعادة التوجيه إلى صفحة الدفع
- التحقق من حالة الدفع
- دعم الدفع المباشر والتسجيل

✅ **صفحة الدفع (Checkout)**
- سلة المشتريات
- حساب الإجمالي
- خيارات الشراء للآخرين
- تكامل مع بوابة الدفع

### 4. لوحة الطالب (Student Dashboard)
✅ **الملف الشخصي**
- عرض معلومات الطالب
- الدورات المسجل فيها
- الشهادات

✅ **التقدم في الدورات**
- متابعة التقدم
- الدروس المكتملة
- الاختبارات والتقييمات

### 5. الشهادات (Certificates)
✅ **إصدار الشهادات**
- طلب الشهادة بعد إكمال الدورة
- التحقق من الإكمال
- عرض الشهادات الحالية

### 6. المناقشات (Discussions)
✅ **منتدى النقاش**
- إنشاء منشورات
- التعليقات والردود
- التفاعل بين الطلاب

## 🔧 الملفات الرئيسية المُحدّثة

### Backend API Routes
1. **`/app/api/auth/login/route.ts`** - تسجيل الدخول
2. **`/app/api/auth/register/route.ts`** - التسجيل
3. **`/app/api/auth/session/route.ts`** - إدارة الجلسات
4. **`/app/api/payments/process/route.ts`** - معالجة الدفع
5. **`/app/api/courses/route.ts`** - جلب الدورات
6. **`/app/api/enrollments/route.ts`** - التسجيل في الدورات

### Frontend Pages
1. **`/app/auth/login/page.tsx`** - صفحة تسجيل الدخول
2. **`/app/auth/register/page.tsx`** - صفحة التسجيل
3. **`/app/checkout/page.tsx`** - صفحة الدفع
4. **`/app/courses/client-page.tsx`** - صفحة الدورات
5. **`/app/student/page.tsx`** - لوحة الطالب

### Core Libraries
1. **`/lib/learnworlds-client.ts`** - عميل LearnWorlds API
2. **`/lib/auth-context.tsx`** - سياق المصادقة
3. **`.env.local`** - متغيرات البيئة

## 🚀 كيفية الاستخدام

### 1. تسجيل الدخول
```
1. اذهب إلى /auth/login
2. اختر "تسجيل الدخول عبر LearnWorlds" للوصول المباشر
3. أو أدخل البريد الإلكتروني وكلمة المرور
4. سيتم توجيهك إلى لوحة الطالب
```

### 2. التسجيل
```
1. اذهب إلى /auth/register
2. اختر "إنشاء حساب عبر LearnWorlds"
3. أو املأ البيانات المطلوبة
4. سيتم إنشاء حسابك في LearnWorlds
```

### 3. شراء دورة
```
1. تصفح الدورات في /courses
2. انقر على "اشترِ الآن"
3. إذا لم تكن مسجلاً، سيطلب منك التسجيل
4. ستُوجّه إلى صفحة الدفع
5. انقر CHECKOUT للتوجه إلى LearnWorlds
6. أكمل عملية الدفع على LearnWorlds
```

## 🔍 التحقق من التكامل

### اختبار API
```bash
# Test Login
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'

# Test Courses
curl http://localhost:3000/api/courses

# Test User Enrollments
curl http://localhost:3000/api/enrollments?userId=USER_ID&courseId=COURSE_ID
```

### Console Logs
تم إضافة `[v0]` logs في:
- عمليات تسجيل الدخول والتسجيل
- معالجة الدفع
- جلب الدورات
- إدارة الجلسات

## ⚠️ ملاحظات مهمة

### 1. البيانات الوهمية (Mock Data)
تم إزالة جميع البيانات الوهمية والآن يتم استخدام LearnWorlds API فقط.

### 2. الأمان (Security)
- جميع المفاتيح في `.env.local`
- الجلسات محمية بـ HTTP-only cookies
- التحقق من التوكنات في كل طلب

### 3. معالجة الأخطاء
- رسائل خطأ واضحة باللغة العربية والإنجليزية
- Fallback URLs في حالة فشل API
- Logging كامل للتتبع

### 4. الأداء (Performance)
- Caching للجلسات
- طلبات API محسّنة
- تحميل تدريجي للبيانات

## 📞 الدعم الفني

إذا واجهت أي مشاكل:

1. **تحقق من Console Logs** - ابحث عن `[v0]`
2. **تأكد من متغيرات البيئة** - تحقق من `.env.local`
3. **اختبر API مباشرة** - استخدم Postman أو cURL
4. **تحقق من LearnWorlds Dashboard** - تأكد من صلاحيات API

## 🎯 الخطوات التالية

1. ✅ اختبار كامل لتسجيل الدخول والتسجيل
2. ✅ اختبار عملية الدفع الكاملة
3. ✅ التحقق من جلب الدورات بشكل صحيح
4. ✅ اختبار SSO مع OAuth providers
5. 📝 إضافة المزيد من الوظائف حسب الحاجة

---

**تم التكامل بنجاح! 🎉**
المنصة الآن متصلة بالكامل مع LearnWorlds API وجاهزة للاستخدام.
